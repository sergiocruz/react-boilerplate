'use strict';

var React = require('react');

module.exports = React.createClass({
  render: function() {
    return (
      /* jshint ignore:start */
      <div>Welcome to the Home Page</div>
      /* jshint ignore:end */
    );
  }
});
